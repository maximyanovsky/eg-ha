package eghomeassignment.common.signals
{
    import flash.display.BitmapData;

    import org.osflash.signals.Signal;

    public class BitmapdataSignal extends Signal
    {
        public function BitmapdataSignal()
        {
            super(BitmapData)
        }
    }
}
