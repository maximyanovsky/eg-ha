package eghomeassignment.models.signals
{
    import eghomeassignment.models.*;
    import org.osflash.signals.Signal;

    public class ImageSignal extends Signal
    {
        public function ImageSignal()
        {
            super(IImageModel);
        }
    }
}
